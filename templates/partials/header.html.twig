<!-- Bar de navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('homepage') }}"><i class="fas fa-home"></i> RECDEC !</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" 
        aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link taille" href="{{ path('decede_new') }}">
                    <i class="fas fa-user-alt-slash"></i>
                    Saisir décédé
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link taille" href="{{ path('decede_list') }}">
                    <i class="fas fa-eye"></i>
                    Voir mes saisies
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link taille" href="{{ path('users_statsfosa') }}">
                    <i class="fas fa-chart-bar"></i>
                    Statistiques de saisie
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link taille" href="{{ path('collecte_equipe') }}">
                    <i class="fas fa-chart-bar"></i>
                    Données collectées par équipe
                </a>
            </li>
            {#
            <li class="nav-item">
                <a class="nav-link taille" href="{{ path('acte_create') }}">
                   <i class="fas fa-user-alt-slash"></i>
                    Saisir acte de décès
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link taille" href="{{ path('actes_index') }}">
                    <i class="fas fa-eye"></i>
                    Voir mes saisies
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link taille" href="{{ path('users_stats') }}">
                    <i class="fas fa-chart-bar"></i>
                    Statistiques de saisie
                </a>
            </li>
            #}
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropDownLink">
                        <i class="fas fa-user-check"></i> {{app.user.fullName}}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropDownLink">
                        <a href="#" class="dropdown-item">Saisir acte</a>
                        <a href="{{ path('decede_list') }}" class="dropdown-item">Mes saisies</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_password') }}" class="dropdown-item">Modifier mot de passe</a>
                        <a href="{{ path('account_profile') }}" class="dropdown-item">Modifier mon profil</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_logout') }}" class="dropdown-item"
                            id="myLinkToConfirm" data-confirm-title="Attention !" 
                            data-confirm-message="Etes-vous sûr de vouloir vous déconnectez ?">
                            Déconnexion
                        </a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link taille" href="{{ path('account_login') }}">
                        <i class="fas fa-sign-in-alt"></i>
                        Connexion <span class="sr-only">(current)</span>
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>   

{# Alertes #}
{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}

{% block javascripts %}
    <script>
        $('#myLinkToConfirm').confirmModal();
    </script>
{% endblock %}